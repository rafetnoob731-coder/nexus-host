{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm install --prefix api",
    "watchPatterns": ["api/**/*", "server.js", "package.json"]
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
